# 🚀 Quick Start: Integration Tests

## ✅ Что создано

**Реальные интеграционные тесты** для проверки всего pipeline!

### Файлы:
- ✅ `tests/integration/test_pipeline_e2e.py` - Тесты (300+ строк)
- ✅ `tests/integration/create_test_video.py` - Генератор тестового видео
- ✅ `tests/integration/README.md` - Полная документация

---

## 🎯 Быстрый старт (3 шага)

### 1. Подготовить тестовое видео

**Вариант A**: Положите свое видео (5-10 сек)
```bash
# Скопируйте короткое видео
copy "C:\path\to\video.mp4" "tests\video\test.mp4"
```

**Вариант B**: Создайте автоматически (требует ffmpeg)
```bash
python tests/integration/create_test_video.py
```

**Вариант C**: Используйте существующее (если есть)
```bash
# У вас уже есть tests/video/test.mp4
```

### 2. Запустить базовые тесты (быстро, без ML)
```bash
pytest tests/integration/ -v
```

### 3. Запустить ML тесты (медленно, требует GPU)
```bash
# Windows PowerShell
$env:RUN_ML_TESTS="1"; pytest tests/integration/ -v

# Linux/Mac
RUN_ML_TESTS=1 pytest tests/integration/ -v
```

---

## 📊 Что тестируется

### Level 1: Basic Tests (✅ Работают без GPU)
```bash
pytest tests/integration/test_pipeline_e2e.py::TestBasicVideoProcessing -v
```

Проверяют:
- ✅ Чтение метаданных видео
- ✅ Извлечение кадров
- ✅ Сборку видео обратно

**Время**: ~5 секунд

### Level 2: ML Tests (⚠️ Требуют GPU)
```bash
$env:RUN_ML_TESTS="1"
pytest tests/integration/test_pipeline_e2e.py::TestMLProcessing -v
```

Проверяют:
- ✅ Real-ESRGAN upscaling
- ✅ RIFE interpolation

**Время**: ~30 секунд

### Level 3: Full Pipeline (🐌 Очень медленно)
```bash
$env:RUN_FULL_TESTS="1"
pytest tests/integration/test_pipeline_e2e.py::TestFullPipeline -v
```

Проверяет:
- ✅ Upscale + Interpolation вместе

**Время**: ~2 минуты

### Level 4: Debug Tests (✅ Быстро)
```bash
pytest tests/integration/test_pipeline_e2e.py::TestDebugMode -v
```

Проверяет:
- ✅ Debug logging работает

**Время**: <1 секунда

---

## 🎬 Если нет тестового видео

### Создать вручную с ffmpeg:
```bash
# 5 секунд, 640x360, 24fps
ffmpeg -f lavfi -i testsrc=duration=5:size=640x360:rate=24 ^
       -pix_fmt yuv420p tests/video/test.mp4
```

### Или просто скопировать любое короткое видео:
```bash
# Главное чтобы было коротким (5-10 сек)
copy "video.mp4" "tests\video\test.mp4"
```

---

## 🐛 Отладка

### Тест не находит видео?
```powershell
# Проверить что видео есть
Test-Path "tests\video\test.mp4"

# Создать директорию
New-Item -ItemType Directory -Force -Path "tests\video"
```

### Тест падает?
```bash
# Запустить с подробным выводом
pytest tests/integration/ -v -s --tb=short

# Запустить один тест
pytest tests/integration/test_pipeline_e2e.py::test_video_info_extraction -v -s
```

### Хотите видеть что происходит?
```bash
# Включить debug mode
$env:DEBUG_PROCESSORS="1"
pytest tests/integration/ -v -s
```

---

## 📈 Что дают эти тесты

### До (только unit тесты):
- ❌ Не проверяли реальную обработку
- ❌ Не тестировали интеграцию
- ❌ Не знали что всё работает вместе

### После (integration тесты):
- ✅ Проверяют реальное видео
- ✅ Тестируют весь pipeline E2E
- ✅ Уверенность что всё работает
- ✅ Можно отлаживать проблемы

---

## ✅ Checklist

- [ ] Положить тестовое видео в `tests/video/test.mp4`
- [ ] Запустить базовые тесты: `pytest tests/integration/ -v`
- [ ] Убедиться что проходят
- [ ] (Опционально) Запустить ML тесты если есть GPU

---

## 📚 Полная документация

См. `tests/integration/README.md` для:
- Подробного описания всех тестов
- Примеров использования
- Best practices
- CI/CD integration
- Troubleshooting

---

**Готово! Начните тестировать!** 🚀

*Создано: 1 декабря 2025*  
*Интеграционные тесты готовы к использованию!*

