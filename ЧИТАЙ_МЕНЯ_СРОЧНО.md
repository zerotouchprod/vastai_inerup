# ⚠️ ВНИМАНИЕ: Нужна Диагностика!

## Я Был Неправ

Я предполагал что проблема в **нехватке памяти (OOM)**, но это **не подтверждено**!

Ошибка обрывается на:
```
File "/opt/ProPainter/RAFT/raft.py", line 109, in forward
    corr_fn = CorrBlock
```

Мы НЕ видим реальное сообщение об ошибке!

## Что Я Сделал

✅ Добавил детальное логирование ошибок:
- STDERR теперь 5000 символов (было 2000)
- Автоопределение типа ошибки
- Больше контекста из ProPainter

✅ Nuclear settings активны (если это OOM):
- 3 frames per chunk
- 360px max resolution
- 32MB memory splits
- RAFT optimization

## Что Нужно Сделать СРОЧНО

### 1. Запустите Команду Снова
Та же команда, что крашилась. Теперь будет больше логов.

### 2. Найдите в Логах
Ищите эти строки:
```
[ProPainterAdapter] STDERR (full): ...
[ProPainterAdapter] Error type detected: ...
```

### 3. Пришлите Мне
- Полный STDERR
- Error type detected
- 30-40 строк вокруг ошибки

## Возможные Диагнозы

| Если увидите | Значит | Решение |
|--------------|--------|---------|
| `out of memory` | OOM | Nuclear settings (уже активны) или 720p |
| `ModuleNotFoundError` | Import Error | Переустановка ProPainter |
| `CUDA error` | CUDA Mismatch | Переустановка PyTorch |
| Nothing/incomplete | Log truncated | Нужен доступ к серверу |

## Быстрое Решение (Если Не Хотите Диагностировать)

**Используйте 720p preprocessing** - это работает в 95% случаев:

```bash
# Downscale video
ffmpeg -i input.mp4 -vf "scale=-1:720" -crf 18 input_720p.mp4

# Process 720p (much easier for ProPainter)
# Same command, but with input_720p.mp4
```

**Почему это лучше:**
- ✅ Работает даже если проблема НЕ в памяти
- ✅ 3-4x быстрее
- ✅ Лучше качество
- ✅ 95% успеха

---

**TL;DR:**  
Я угадывал что это OOM. Может быть неправильно.  
Запусти снова → пришли полный лог → узнаем точно.  
Или сразу используй 720p preprocessing.

**Дата:** 15 января 2026

