fix: use snapshot_download API for model download (v3)

The huggingface-cli module doesn't exist in huggingface_hub package.
Use direct Python API with snapshot_download() instead.

Changes:
- docker/Dockerfile.gen: Use snapshot_download() instead of CLI
- FIX_V3_FINAL.md: Added comprehensive fix documentation

Previous attempts:
- v1: /opt/venv/bin/huggingface-cli - not found in PATH
- v2: python -m huggingface_hub.commands.huggingface_cli - module doesn't exist
- v3: python -c "from huggingface_hub import snapshot_download" - WORKS!

This resolves the "No module named 'huggingface_hub.commands'" error.

Build command:
```bash
docker builder prune -a -f  # Clear cache first!
docker build -f docker/Dockerfile.gen -t video-gen:latest --progress=plain .
```
