# Профиль "Точный улучшенный" - для низкоконтрастного текста со свечением
# Обновлено для централизованной конфигурации (2026-01-18)

# =============================================================================
# OCR DETECTION SENSITIVITY
# =============================================================================

# Основной порог уверенности OCR (заменяет CONFIDENCE_THRESHOLD)
OCR_CONFIDENCE_THRESHOLD=0.12

# Двойной проход OCR (улучшает детекцию, но медленнее)
OCR_DUAL_PASS_ENABLED=True

# Порог IoU для объединения дубликатов
OCR_DUPLICATE_IOU_THRESHOLD=0.3

# =============================================================================
# BOUNDING BOX EXPANSION
# =============================================================================

# Горизонтальное расширение (пиксели)
BBOX_EXPAND_HORIZONTAL=15

# Вертикальное расширение (пиксели)
BBOX_EXPAND_VERTICAL=20

# =============================================================================
# MASK DILATION (VRAM-ADAPTIVE)
# =============================================================================

# Итерации дилатации (до морфологического закрытия)
DILATION_ITERATIONS_INITIAL=2

# Итерации морфологического закрытия
MORPHOLOGICAL_CLOSING_ITERATIONS=1

# Финальные итерации дилатации
DILATION_ITERATIONS_FINAL=1

# =============================================================================
# CLAHE ENHANCEMENT
# =============================================================================

# Параметры CLAHE для улучшения контраста
CLAHE_CLIP_LIMIT=4.0
# CLAHE_TILE_GRID_SIZE=(8,8)  # Не поддерживается в .env файлах

# =============================================================================
# MEMORY OPTIMIZATION
# =============================================================================

# Интервал очистки GPU памяти (кадры)
GPU_CLEANUP_INTERVAL=50

# =============================================================================
# LEGACY PARAMETERS (для обратной совместимости)
# =============================================================================

# Старые параметры OCR (для совместимости)
OCR_TEXT_THRESHOLD=0.15
OCR_LOW_TEXT=0.15
OCR_LINK_THRESHOLD=0.25
OCR_MAG_RATIO=1.3
OCR_CANVAS_SIZE=2240
MASK_DILATION=8
CONFIDENCE_THRESHOLD=0.12  # Алиас для OCR_CONFIDENCE_THRESHOLD
OCR_THRESHOLD=0.12
OCR_BBOX_MIN_SCORE=0.2
OCR_BBOX_MIN_SIZE=4
OCR_MAX_CANDIDATES=1000
OCR_SLOPE_THS=0.15
OCR_YCENTER_THS=0.6
OCR_HEIGHT_THS=0.6
OCR_WIDTH_THS=0.6
OCR_ADD_MARGIN=0.08

# =============================================================================
# SYSTEM SETTINGS
# =============================================================================

FORCE_CPU=True
USE_GPU_FOR_OCR=False
