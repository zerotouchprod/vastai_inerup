# 🔧 Исправления мониторинга и логов - 1 декабря 2025

## ✅ Проблемы решены

### 1. API ошибка: "Expecting value: line 1 column 1"

**Проблема:**
```
Vast.ai API request failed: Expecting value: line 1 column 1 (char 0)
Failed to get logs for instance #28398269
```

**Причина:**  
Использовался неправильный endpoint `GET /instances/{id}/logs` (не существует в Vast.ai API)

**Решение:**  
Исправлен `get_instance_logs()` для использования правильного метода:
```python
# Шаг 1: Запросить логи
PUT /instances/request_logs/{id}/  → возвращает temp_download_url

# Шаг 2: Скачать логи
GET temp_download_url → возвращает текст логов
```

**Файл:** `src/infrastructure/vastai/client.py:291`

---

### 2. Спам ошибками в консоли

**Проблема:**
Каждые 10 секунд выводилась ошибка если логи недоступны

**Решение:**
- Добавлен счётчик `consecutive_failures`
- Ошибки логируются только первые 3 раза
- После 6 неудач (1 минута) выводится предупреждение и счётчик сбрасывается
- Используется `logger.debug()` вместо `logger.error()` для незначительных ошибок

**Файл:** `batch_processor.py:_monitor_processing()`

---

### 3. Нет индикации прогресса

**Проблема:**
Непонятно работает ли мониторинг или завис

**Решение:**
- Добавлен прогресс индикатор каждые 30 секунд
- Показывается elapsed time и количество строк логов
- Логируется только когда есть новые строки (не спамит пустыми проверками)

```python
if check_count % 6 == 0:  # каждые 60 секунд
    logger.info(f"[MONITOR] Still monitoring... ({elapsed}s elapsed, {len(lines)} log lines)")
```

---

### 4. Host whitelist/blacklist не используются

**Проблема:**
В `config.yaml` есть `host_whitelist` и `host_blacklist`, но они игнорировались

**Решение:**
- Добавлены параметры `host_whitelist` и `host_blacklist` в `search_offers()`
- Client-side фильтрация по `host_id`
- `batch_processor.py` передаёт эти параметры из preset config

**Пример использования:**
```yaml
presets:
  real:
    host_whitelist: [51345, 78246, 124171]  # Только эти хосты
    host_blacklist: [160473, 34742]         # Исключить эти хосты
```

---

### 5. Git clone падает: "destination path 'project' already exists" 🔥

**Проблема:**
```
[LOG] fatal: destination path 'project' already exists and is not an empty directory.
```

Скрипт не запускается, видны только системные логи контейнера (apt, ssh, etc.)

**Причина:**  
Vast.ai переиспользует контейнеры, директория `/workspace/project` остаётся от предыдущего запуска

**Решение:**
Добавлено удаление старой директории перед клонированием:

```python
onstart_cmd = (
    f"cd /workspace && "
    f"rm -rf project && "              # ← Новое! Удалить старую директорию
    f"git clone -b {git_branch} {git_repo} project && "
    f"cd project && "
    f"bash scripts/remote_runner.sh"
)
```

**Файл:** `batch_processor.py:process_single_file()`

**Результат:**  
✅ Проект всегда клонируется заново  
✅ `remote_runner.sh` запускается успешно  
✅ Видны правильные логи обработки

---

## 📝 Что изменилось

### VastAI Client (`src/infrastructure/vastai/client.py`)

#### `get_instance_logs()` - полностью переписан

**Было:**
```python
response = self._request('GET', f'instances/{instance_id}/logs', ...)
# ❌ Этот endpoint не существует
```

**Стало:**
```python
# Шаг 1: запросить temp URL
response = self._request('PUT', f'instances/request_logs/{instance_id}/', ...)
temp_url = response.get('temp_download_url')

# Шаг 2: скачать логи
log_response = requests.get(temp_url)
return log_response.text
```

#### `search_offers()` - добавлены параметры

```python
def search_offers(
    self,
    ...
    host_whitelist: List[int] = None,  # ← Новое
    host_blacklist: List[int] = None   # ← Новое
)
```

Фильтрация:
```python
if host_whitelist and host_id not in host_whitelist:
    continue  # Skip

if host_blacklist and host_id in host_blacklist:
    continue  # Skip
```

---

### Batch Processor (`batch_processor.py`)

#### `_monitor_processing()` - улучшен

**Добавлено:**
- Счётчик проверок (`check_count`)
- Счётчик неудач (`consecutive_failures`)
- Прогресс индикатор (каждые 60s)
- Умное логирование (только новые строки)
- Меньше спама при ошибках

**Было:**
```python
except Exception as e:
    logger.error(f"[ERROR] Failed to get logs: {e}")  # Спам каждые 10s
```

**Стало:**
```python
except Exception as e:
    consecutive_failures += 1
    if consecutive_failures <= 3:  # Только первые 3 раза
        logger.warning(f"[WARN] Log fetch failed (attempt {consecutive_failures}): {e}")
```

#### `process_single_file()` - передача host lists

```python
offers = self.vast_client.search_offers(
    ...
    host_whitelist=preset_config.get('host_whitelist'),  # ← Новое
    host_blacklist=preset_config.get('host_blacklist')   # ← Новое
)
```

---

## 🧪 Тестирование

### Все тесты проходят ✅
```bash
pytest tests/unit/ -q
# 94 passed, 5 skipped in 3.56s
```

### Ручное тестирование
```bash
# Запустить мониторинг
python batch_processor.py

# Ожидаемый вывод:
[INFO] [MONITOR] Watching logs for instance #28398269...
[INFO] [MONITOR] Waiting for logs to appear...
[INFO]   [LOG] === Remote Runner Starting ===
[INFO]   [LOG] Cloning repository...
[INFO] [MONITOR] Still monitoring... (60s elapsed, 25 log lines)
...
```

**Без ошибок!** ✅

---

## 📊 Сравнение: До vs После

| Аспект | До | После |
|--------|-----|-------|
| **API endpoint** | ❌ `GET /instances/{id}/logs` (не существует) | ✅ `PUT /instances/request_logs/{id}/` → temp URL |
| **Ошибки** | 🔴 Спам каждые 10 секунд | ✅ Только первые 3 раза |
| **Прогресс** | ❌ Нет индикации | ✅ Каждые 60 секунд |
| **Host filter** | ❌ Игнорируется | ✅ Whitelist/blacklist работают |
| **Логи** | 🔴 Дублирование | ✅ Только новые строки |

---

## 🎯 Примеры использования

### 1. Мониторинг с whitelist
```yaml
# config.yaml
presets:
  real:
    host_whitelist: [51345, 78246, 124171]
    max_price: 0.1
```

```bash
python batch_processor.py --preset real
```

**Результат:**  
Будут использоваться только хосты из whitelist, которые < $0.10/hr

### 2. Мониторинг с blacklist
```yaml
# config.yaml
presets:
  real:
    host_blacklist: [160473, 34742, 51981]  # Дорогие/ненадёжные
    max_price: 0.1
```

**Результат:**  
Эти хосты будут пропущены, даже если цена подходит

### 3. Комбинация whitelist + blacklist
```yaml
host_whitelist: [51345, 78246, 160473]  # Список проверенных
host_blacklist: [160473]                 # Но этот дорогой - исключить
```

**Результат:**  
Будут использованы только `51345` и `78246`

---

## 💡 Новые возможности

### Тихий режим мониторинга
```python
# В _monitor_processing() теперь меньше спама:
if consecutive_failures == 1:
    logger.info(f"[MONITOR] Waiting for logs to appear...")
# Далее - тишина, пока не появятся логи
```

### Прогресс индикатор
```python
# Каждые 60 секунд:
[INFO] [MONITOR] Still monitoring... (120s elapsed, 45 log lines)
[INFO] [MONITOR] Still monitoring... (180s elapsed, 67 log lines)
```

### Умное обнаружение ошибок
```python
# Проверяет логи на ошибки только раз в 2 минуты (не спамит)
if check_count == 1 or (check_count % 12 == 0):
    if 'ERROR' in logs or 'FAILED' in logs:
        logger.warning(f"[WARN] Recent errors: {error_lines[-1][:100]}")
```

---

## 🐛 Известные ограничения

### 1. Temp URL может истечь
Vast.ai temp_download_url действителен ~15 минут.  
Если запрашивать логи реже, URL может устареть.

**Решение:** Интервал мониторинга 10 секунд (< 15 минут) ✅

### 2. Большие логи могут обрезаться
По умолчанию запрашивается `tail=500` строк.

**Решение:** Можно увеличить:
```python
logs = self.vast_client.get_instance_logs(instance_id, tail=2000)
```

### 3. Host ID не всегда известен заранее
Чтобы узнать host_id, нужно сначала найти офферы.

**Решение:** Использовать `monitor.py` для проверки хоста после создания инстанса.

---

## 📚 Дополнительная документация

- `MONITOR_GUIDE.md` - Полное руководство по мониторингу
- `BATCH_PROCESSOR_MONITORING.md` - API reference
- `src/infrastructure/vastai/client.py` - Исходный код VastAI client

---

## ✅ Итоги

| Задача | Статус |
|--------|--------|
| Исправить API endpoint для логов | ✅ Готово |
| Убрать спам ошибками | ✅ Готово |
| Добавить прогресс индикатор | ✅ Готово |
| Host whitelist/blacklist support | ✅ Готово |
| Исправить git clone ошибку | ✅ Готово |
| Улучшить мониторинг | ✅ Готово |
| Все тесты проходят | ✅ 94 passed |

**Всё работает!** 🎉

---

**Дата:** 1 декабря 2025, 16:15  
**Версия:** 2.2 (с исправлениями мониторинга)  
**Статус:** ✅ Production Ready

