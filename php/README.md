Vast.ai Laravel service

Minimal service that provides a Vast.ai client and endpoints to search offers and launch instances.

Usage:
- Install dependencies: composer install
- Include `php/` in your Laravel app or run as separate microservice with PHP built-in server.

.env.example contains recommended env vars.

